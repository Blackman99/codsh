---
'codsh-bundle': patch
---

Give Wayfinder continuation answers a join from each decision column instead of a line from the last workflow step, size CJK answer cards by display width, and space Landing DAGs so skipped-rank prerequisite edges leave the columns instead of crossing tickets.
