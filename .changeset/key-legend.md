---
"codsh-bundle": patch
---

A key legend sits under the box — `? shortcuts · ⇧Tab plan · Ctrl+T todos · Ctrl+O folds` — and stays while you type, where the placeholder used to leave with the first character. It lives on the hint row, so a hint, a copy toast, a find, or a hover replaces it instead of adding a row; the box does not move, and under a selector the row steps aside.
