---
'codsh-cli': minor
'codsh-bundle': minor
---

Add substitute login, logout, and managed-config setup to `codsh --rust`. Identity tokens stay isolated from model keys and other services; unsigned or unverifiable organization policy is refused, and official grok.com entitlements are not reproduced.
