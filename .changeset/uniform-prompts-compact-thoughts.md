---
'codsh-bundle': patch
---

Use the same filled background and text inset for canned command prompts (`/ship`, `/init`, and custom commands) as ordinary user messages. Keep streaming and collapsed thinking to one row, retaining padded reasoning only when expanded.
