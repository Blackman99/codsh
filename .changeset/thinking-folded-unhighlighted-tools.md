---
'codsh-bundle': patch
---

Fold thinking by default and drop the tool-card highlight. A thought still streams as a `thinking…` head that becomes its clock, but the deliberation stays behind a click or Ctrl+O; tool rows keep their one-line form without a panel fill or amber title.
