---
'codsh-bundle': patch
---

Fix false `/ship` snapshot-change stops after Gate 1 by comparing saved field values independently of JSON key order, while continuing to reject missing, corrupt, or changed snapshots.

Recognize local Wayfinder `decision-NN-slug.md` tickets and Markdown-list metadata so each ticket and its status appear in the live TTY and web panorama. Share the filename and Status handling with claim recovery, preserve closed decisions, and specify the local ticket format in the injected Wayfinder contract. Stack the web graph and details on narrow screens so the graph remains usable.
