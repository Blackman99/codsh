---
'codsh-bundle': patch
---

Keep `/ship` in the same invocation after Gate 2 while a Landing wave is in flight: the working line stays on, the panorama teaser reports `in-flight`, and each landing child receives the TDD contract. Do not invent a worktree directory or dispatch when `git worktree add` fails.
