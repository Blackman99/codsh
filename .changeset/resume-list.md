---
"codsh-bundle": minor
---

`/resume` offers the folder you are standing in, and folds every other folder behind one row that opens the rest. Rows are ordered by when a session was last touched rather than when it was created, and each names its title, that age, how many messages it holds, and — only when the session belongs to another folder — which one, `~`-shortened.
