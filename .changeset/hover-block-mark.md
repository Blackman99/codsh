---
"codsh-bundle": patch
---

Hover no longer jumps the input box: the readout borrows the hint or status row instead of adding one. The block under the pointer fills like opencode's panel, so the paragraph is obvious without underlining every letter.
