---
"codsh-bundle": patch
---

A click on the pinned `/ship` plan — or the todo readout — now opens the list and closes it again, the same way a fold works. Ctrl+T still does it from the keyboard. The teaser names both: `click or Ctrl+T opens the list`.
