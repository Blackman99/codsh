---
'codsh-bundle': patch
---

A live Conflict-resolution child can fill merge hunks without Alignment Gate refusing the writes as unmapped landing work. Invented `supports`, missing Track mappings, and path aliases no longer stall `/ship` on leftover conflict markers.
