---
'codsh-cli': minor
'codsh-bundle': minor
---

Fork and rewind conversations through dsh seed/projection without restoring files. Isolated `codsh --rust` supports `/rewind`, `/undo`, `/fork`, `--fork-session`, and refuses `--restore-code`.
