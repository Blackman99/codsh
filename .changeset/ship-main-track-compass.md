---
'codsh-bundle': minor
---

`/ship` keeps a frozen Main Track as the later-phase compass, asks `ship · occupancy` before replacing an unrelated `/goal`, and shows that compass on `/goal` during a run.
