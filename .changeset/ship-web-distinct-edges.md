---
'codsh-bundle': patch
---

Draw ticket relations as orthogonal lanes with staggered exits and entries. Skipped-rank edges split left and right of the phase so concurrent prerequisites no longer share one horizontal or vertical run.
