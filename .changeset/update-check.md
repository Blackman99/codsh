---
"codsh-bundle": minor
"codsh-cli": minor
---

Tell a session when a newer codsh is published, install it on `/update` or `codsh update`, and answer `codsh --version` for the pair instead of the runtime it launches.
