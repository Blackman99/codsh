---
"codsh-bundle": patch
---

Completion matches a fragment anywhere in the name, not only a prefix. The menu sits above the box so opening it cannot lift the prompt. A finished `/command` or `$skill` is coloured in the box.
