---
'codsh-cli': patch
'codsh-bundle': patch
---

Write isolated dsh credentials owner-only, apply overlay `api_key`, inspect the telemetry values actually forced off for dsh, and keep first-run config fields visible.
