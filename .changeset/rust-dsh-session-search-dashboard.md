---
'codsh-cli': minor
'codsh-bundle': minor
---

Search, rename, and resume dsh sessions from the welcome list, `/resume`, the agent dashboard, and `codsh --rust sessions`. Manual titles outrank generated titles. A refused switch keeps the current write owner, and the launcher forwards the dashboard startup settings.
