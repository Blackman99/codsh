---
"codsh-bundle": minor
---

Add semantic conversation navigation in the interactive TTY: Shift+Left and Shift+Right move between real user turns, while `/jump` provides a searchable preview that restores the previous viewport when cancelled.
