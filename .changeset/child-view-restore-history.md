---
'codsh-bundle': patch
---

Esc from a Child view restores the covered parent transcript — history, scroll offset, and open folds — instead of replaying the log from scratch and landing at the tail.
