---
'codsh-cli': minor
'codsh-bundle': minor
---

Add explicit `codsh --rust import` for selected legacy dsh providers and preferences. Preview lists conversions, conflicts, and unsupported items from current `settings.yaml` sources without copying tokens, credential files, or trust grants; apply writes only into the isolated Home.
