---
'codsh-cli': patch
'codsh-bundle': patch
---

Refuse /rewind during a live turn, accept /fork --no-worktree, apply the fork model only on fork, and store confirm/fork UI prefs in `$GROK_HOME/config.toml`.
