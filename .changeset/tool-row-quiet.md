---
'codsh-bundle': patch
---

Quiet the tool-row column. A finished success bullet is dim instead of green, so the trailing tick carries the pass; consecutive one-line cards on a TTY open with a blank between them so a long run is readable.
