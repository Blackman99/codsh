---
'codsh-cli': minor
'codsh-bundle': minor
---

Add managed defaults, locked requirements, and workspace folder trust to `codsh --rust`. Inspectable origins keep locked values from being bypassed; untrusted project Hooks/plugins/instructions stay inactive until an explicit grant.
