---
'codsh-cli': patch
'codsh-bundle': patch
---

Cancel an in-flight web search or fetch from dsh without returning a late body. Redirects recheck the full allow entry, an http proxy tunnels with CONNECT, and managed web defaults stay user-overridable under a requirements pin.
