---
"codsh-bundle": minor
---

`$` searches user-invocable skills the way `/` searches commands. Pick one and it stays in the prompt; submit rewrites known `$name` tokens into the `/name` gesture dsh injects.
