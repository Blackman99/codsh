---
'codsh-bundle': patch
---

Lay out sibling dependents of one prerequisite in the same rank, each with its own ticket/answer cluster, so the flowchart forks on a short path. Do not stack those tickets into one column, wrap the prerequisite around the phase, invent research-ticket user answers, or repeat an unlinked copy of a question already shown on its ticket.
