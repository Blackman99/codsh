---
'codsh-bundle': minor
---

Compile a sealed Mission Contract at `/ship` Gate 1 Confirm (REQ/NEG/ACC ids in `.scratch/<slug>/mission.contract.json`), prepend it on later phases, and ignore Main Track rewrites against the seal.
