---
'codsh-bundle': patch
---

Keep `/ship` plan and land-chip chrome following spec checkboxes after a mid-run interrupt or idle stop, so a later typed continue still updates `plan k/n` without another `/ship`.
