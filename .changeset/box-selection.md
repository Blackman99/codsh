---
"codsh-bundle": patch
---

Click-and-drag now selects text in the input box the same way it does in the transcript: press anchors, motion extends, release copies, and the span stays marked until the next click or key. A click that never moved still just places the cursor. Typing, paste, and delete replace the selected span.
