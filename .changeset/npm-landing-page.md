---
"codsh-cli": patch
---

Lead the npm page with what `/ship` does and show the capture, before the launcher's own mechanics. The page is what an npm search lands on, and it opened on the sentence "This package bundles nothing".
