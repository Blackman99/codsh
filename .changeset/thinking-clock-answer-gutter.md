---
'codsh-bundle': patch
---

Draw one connecting `│` down every transcript row — prompts, thinking, tools, answers, notices, and blank separators — and colour it by what the row is. Drop the mixed `›` / `✻` / `·` gutter glyphs so the left rail no longer breaks between blocks.
