---
"codsh-bundle": patch
---

Give the status row back when the pointer leaves the window or moves onto the chrome. A hover readout had been sticky: leaving the transcript (or the window) kept showing `thinking · N lines · click to expand` instead of the model/tokens line.
