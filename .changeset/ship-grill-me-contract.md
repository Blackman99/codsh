---
'codsh-bundle': minor
'codsh-cli': minor
---

`/ship` now follows the grill-me skill as a contract: recon first, batched frontier rounds with recommended answers, the `ship · grill` header, and an exhaustion handshake before the spec.
