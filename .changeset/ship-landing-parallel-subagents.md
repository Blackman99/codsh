---
'codsh-bundle': patch
---

Align parallel landing subagent writes with their respective ticket in the Alignment Gate, and register landing child sessions in the subagent roster with start and end lifecycle events so they appear in the subagent roster list (`Ctrl+H`) and status chrome.
