const scenarios={
  136:['a temporary repository with a known file and an approval boundary','request read then denied and approved edit','file bytes and diff agree with tool result; denial changes nothing','reject stale approval and malformed patch without partial writes'],
  137:['a delayed local model response and retained draft/queue','clear draft, cancel turn, then resume input','draft clearing and actual cancellation are distinct; unrelated queued input survives','interrupt before first activity and during a tool; teardown leaves no running child'],
  138:['two synthetic persisted sessions and one running owner','new, continue, load and reconnect by explicit session identity','the selected conversation and single writer are preserved','reject stale owner and interrupted external effect without duplicate execution'],
  139:['isolated user/project/managed configs with distinct values','set the inventoried key or enter the first-run flow','effective values and origins are inspectable and match the declared precedence','invalid values and missing credentials produce actionable errors, never hidden defaults'],
  140:['a local provider advertising an explicit model/protocol/effort catalog','select the inventoried model option and submit a deterministic request','the wire request uses that exact model, backend and supported effort','unsupported capability, auth expiry and malformed stream fail without fallback'],
  141:['conflicting signed requirements, managed defaults and untrusted project assets','attempt the inventoried override through CLI, UI and ACP','locked requirements win and untrusted executable assets stay inactive','invalid signature, unreadable pin and attempted bypass fail closed'],
  142:['allow, ask and deny rules including a hard deny','exercise the inventoried permission mode and remembered approval','explicit deny survives automatic approval and scope matches the chosen rule','cancel, stale approval and blocked hook do not authorize execution'],
  143:['protected files outside an allowed temporary workspace','attempt reads, writes, renames through tools and shell children','actual protected state is unchanged and confinement outcomes are reported','unsupported or failed enforcement refuses execution'],
  144:['isolated processes, secrets and a controlled forbidden network listener','attempt the inventoried child/network/environment operation','process and network restrictions are enforced, not only labeled','unsupported kernel enforcement refuses the requested operation'],
  145:['an installed product, empty isolated home and the exact inventoried command/flag','invoke valid, alias and invalid forms, including pipe/non-TTY','help, completion, stdout/stderr and exit status match the frozen contract','missing arguments, unknown options, SIGINT and SIGTERM follow recorded error behavior'],
  146:['a deterministic loopback model fixture emitting text, thought and tool frames','submit via each inventoried output format and flag combination','machine output parses, event order and terminal event agree with the reference','partial stream and malformed data produce failure and never invented usage'],
  147:['an external JSON-RPC client and isolated session owner','initialize and exercise this exact method/metadata key through its public transport','capabilities, identifiers, streamed events and observable result match the recorded protocol','unknown method, malformed input, cancellation and stale session fail explicitly'],
  148:['two clients attached to one isolated runtime','disconnect and reconnect while one real action is running','one executor and durable writer remain; views converge','stale requests cannot duplicate external effects'],
  149:['a real PTY with draft, queued text and a pending approval','set ui.screen_mode, restart plain grok, test session-only CLI overrides, then switch fullscreen/minimal and resize in place','the persisted default takes effect on restart while CLI/in-place overrides do not rewrite it; session, draft, queue and approval survive; native history and alternate screen follow the chosen mode','mode-only commands refuse with the correct remedy; invalid defaults are explicit and teardown restores the terminal'],
  150:['a real PTY composer with multiline Unicode, history and attachments','perform the inventoried editing gesture in simple and Vim modes','draft text, caret, history and attachment identities match the reference','large paste, escape/cancel and external editor failure preserve unrelated input'],
  151:['a delayed turn with consecutive plain follow-ups, queued commands and an aside','toggle combine_queued_prompts and follow_up_behavior, then edit, reorder, cancel, steer or send-now the inventoried item','actual provider requests and turn counts show merging only for eligible consecutive plain follow-ups when enabled; delivery order, transcript identity and main/aside separation match the reference','commands and ineligible items are not accidentally merged; cancel and session switch neither lose queued input nor duplicate execution'],
  152:['a long deterministic transcript in narrow and wide PTYs','perform the inventoried focus/search/mouse/navigation gesture','selection, reading position and copy target match the reference','dismiss and resize restore the prior reading location without modifying draft'],
  153:['deterministic Markdown, diff, code, diagrams, Unicode and long tool output','open, fold and view the inventoried content in both modes','all original content remains readable; rendered cards and raw copy agree','partial/malformed output and narrow resize do not lose content'],
  154:['a real PTY, deterministic rendered content and isolated appearance/status/animation configuration','change or reset the inventoried renderer setting, reopen its surface, stream content and resize','actual frame cadence, layout, color, folds, status-line output and persistence match the setting scope, without altering raw content','invalid script/value, narrow resize, disabled animation and modal dismissal do not corrupt configuration or lose prompt/transcript data'],
  155:['controlled terminal/clipboard helpers and real local/SSH/tmux-like PTYs with active and idle sessions','exercise the exact doctor, wrap, copy, notification or global quit gesture','clipboard delivery or explicit fallback is truthful; quit exits with terminal modes restored and the expected session/task lifecycle','unsupported protocol, interrupted child, pending modal and disconnect do not strand terminal modes or processes'],
  156:['temporary visible/hidden files with known line ranges','attach the inventoried path/range through completion or drop','the intended bytes and attachment identity reach the model','missing path, excluded file and cancellation are explicit'],
  157:['synthetic image bytes and a provider with explicit vision capability','paste, preview, remove and submit the image','preview and submitted content refer to the same image','text-only model and decoder failure are explicit rather than dropping input'],
  158:['a fake audio device/provider and explicit recording consent','exercise hold/toggle capture, enable/disable Ctrl+Space and F8, invoke /voice with keybindings disabled, and set a session STT language override','actual recording start/stop follows the selected gesture; ui.voice_stt_language overrides voice.language in provider requests, /voice remains available, and text reaches only the intended composer/service','missing device, permission denial, unsupported key-release mode, invalid language and cancellation are actionable without unintended recording'],
  159:['synthetic sessions across directories with known titles, timestamps, running agents and dashboard peek/dispatch inputs','list, search, rename, pin, group, enter/leave the dashboard, select an agent and perform the inventoried dashboard gesture','identity, ordering, focus, selected-session action and dispatch/peek destination match the reference without changing another session','duplicate titles, missing session, cancellation, delete confirmation and inactive selection do not mutate unrelated histories'],
  160:['a saved conversation, independently edited file and recording providers with distinct default and fork models','fork with ui.fork_secondary_model set/unset; rewind with confirm_before_rewind enabled/disabled and accept/dismiss confirmation','the secondary provider request uses the configured fork model or documented main-model default; conversation projection changes only after required confirmation and without implicit file restoration','dismissal leaves history unchanged; unavailable fork model, explicit restore-code and invalid boundaries are separately confirmed/tested'],
  161:['a long synthetic conversation and deterministic summary provider','inspect context then trigger manual/automatic compaction','progress, retained content and projected usage agree across interfaces','cancellation, provider failure and resume do not invent lost context'],
  162:['two synthetic session histories and a controlled share endpoint','export, delete, share or inspect disk usage for the selected session','only selected data changes or leaves the machine with explicit intent','cancel and failed upload preserve unrelated sessions and local data'],
  163:['trusted and untrusted compatible instruction, skill and agent fixtures','discover and invoke the inventoried asset/collision variant','source precedence, substitutions and restricted capabilities are observable','malformed asset and untrusted executable content fail without privilege gain'],
  164:['local hook executables returning accept, block and malformed results','trigger each inventoried lifecycle event','hook input/output and blocking behavior match the contract','timeout, cancellation and hard block survive automatic approval'],
  165:['a local git/archive marketplace and untrusted plugin','install, update, remove and configure the inventoried plugin source','manifest, provenance, trust and resulting files agree','failed update and denied trust leave a recoverable previous installation'],
  166:['a plugin contributing commands, skills and settings','enable and disable its inventoried contribution','discovery, collision badges and active capabilities change together','disable/uninstall removes stale capabilities and cannot leave executable grants'],
  167:['a local stdio MCP fixture with tool discovery and failures','initialize, lookup, invoke and restart the inventoried integration','real calls and initialization state agree with tool results','malformed output, transport loss and disabled tools fail explicitly'],
  168:['a controlled remote MCP/OAuth fixture with content and elicitation','authenticate, read structured content and answer the inventoried request','consent, typed content and authentication destination are preserved','decline, expired token and cancellation do not silently approve'],
  169:['a temporary source tree with hidden files, Unicode and line ranges','invoke the exact inventoried file/search/navigation tool','result paths, ranges and truncation match actual file contents','missing path, invalid query and denied file access are accurate'],
  170:['a temporary working directory and deterministic child process','run the inventoried shell/PTY operation with input and output','cwd, environment, exit code and captured output match the child','timeout, stop and process-tree cleanup leave no stray owned process'],
  171:['a controlled search/fetch service and URL policy','query or fetch through the inventoried route','sources, redirects, body and truncation are traceable to actual responses','auth page, private address, rate limit and cancellation are not reported as success'],
  172:['a deterministic parent and restricted child model fixture','launch the inventoried agent type/model/task','a real dsh child executes and its progress/transcript matches output','denied capability, child failure and stop produce truthful terminal state'],
  173:['an active and a completed child with retained transcript','message, view or resume the inventoried child','only the selected child receives the message and continuation history','stale child ID, cancellation and process restart cannot fabricate a resumed action'],
  174:['a temporary git repository with tracked and untracked changes','create, inspect, apply and remove the inventoried worktree type','isolation and result application preserve unrelated changes','dirty-tree conflict, cancellation and cleanup failure are recoverable'],
  175:['a deterministic long child command owned by one session','background, inspect output, wait and stop the inventoried task','status/output and completion wake reach the correct owner once','disconnect, owner exit and kill distinguish interrupted from completed'],
  176:['a local script emitting bounded completion and failure events','start and stop the inventoried monitor','events wake the owning session without duplicate work','failure is immediate; cancellation and timeout stop the owned process'],
  177:['a fake clock and isolated scheduler registry','create, update, list and cancel the inventoried schedule','interval clamping, phase, fire-immediately and expiry match the reference','unknown ID and session/process restart report correct durability limitations'],
  178:['an isolated durable and process-local scheduler registry','restart the owner and inspect each scheduled occurrence','durability, expiry and missed-fire behavior match actual reference results','uncertain in-flight effects remain unknown rather than automatically replayed'],
  179:['single/multi-choice questions, write-in answers and a saved plan','answer or dismiss the inventoried question/plan/todo interaction','only the intended decision settles and no queued prompt is consumed as an answer','cancellation and plan rejection do not start implementation'],
  180:['a bounded goal with deterministic completion evidence','set, pause, resume and clear the inventoried goal control','goal budget and verification gate reflect actual work','failed verification and missing evidence leave it incomplete'],
  181:['a compatible Rhai script with a deterministic child response','run the inventoried host operation through dsh','script arguments and retained results match the host contract','compile error, child failure and stop are explicit'],
  182:['a Rhai parallel panel at and beyond the configured budget','admit the inventoried concurrent child calls','admission count, concurrency and barriers honor the frozen limits','over-budget panel launches no partial unauthorized children'],
  183:['a workflow with committed and uncommitted host results','pause, stop and resume the inventoried run','same-process continuation uses the original script and committed results','process restart remains interrupted; uncertain side effects are not exactly-once'],
  184:['built-in, user, project and plugin workflow catalogs','list and select the inventoried workflow','source, collision handling and launch arguments identify the correct script','invalid script or missing catalog does not claim a runnable workflow'],
  185:['synthetic global and workspace memories','list, save, search and forget the inventoried note','scope and on-disk content match explicit user intent','disabled memory and unknown note do not leak or silently retain data'],
  186:['queued synthetic memory observations and configured model routes','capture, flush or consolidate the inventoried operation','provider destination, cost and retained notes are observable','lease conflict, provider failure and cancellation preserve recoverable state'],
  187:['a replaceable local image-generation/edit service','request the inventoried image operation with references','actual output artifact, aspect ratio and reference identity agree','invalid input, service error and cancellation leave no fabricated image'],
  188:['a replaceable video service with deterministic job states','submit the inventoried image/reference/keyframe video request','job state, duration, voice/keyframe indexing and output artifact agree','conflicting frames, unsupported resolution and cancellation fail explicitly'],
  189:['a configurable local identity/management provider','exercise the inventoried auth method or enterprise endpoint','token routing, renewal and organization restriction match the declared contract','revoked/expired tokens and invalid policy fail closed without automatic credential transfer'],
  190:['a separately configured remote workspace service and local workspace','connect and execute the inventoried remote operation','remote paths, identity, permissions and actual effects remain distinct from local ones','disconnect and remote restart expose uncertain effects without duplicate execution'],
  191:['a controlled clone backend and temporary target directories','run the inventoried clone/Grove/worktree variant','actual repo, progress, depth and cwd match the selected backend','directory conflict, unavailable platform mount and cancellation clean up safely'],
  192:['disabled telemetry and a local feedback/trace collector','toggle or explicitly submit the inventoried privacy/service control','only consented data reaches the configured destination; drafts stay local','failed submission and auth expiry do not trigger unconsented retry/upload'],
  193:['synthetic legacy and compatible foreign configuration with provider/settings values','explicitly import or dismiss the selected configuration through welcome/settings','copied effective settings preserve source provenance and leave the original unchanged','invalid data, canceled import and credentials or trust grants are not silently copied'],
  194:['synthetic legacy and compatible foreign-session records with source hashes','explicitly import selected session copies into a separate Home/Profile','supported content and provenance survive without modifying the originals','unsupported records and partial import are reported; rollback uses untouched originals'],
  197:['provider responses with known, missing and partial token/cost fields','inspect the inventoried usage view and machine format','usage, cache and cost remain accurate or explicitly unknown/incomplete','missing provider usage is never presented as verified free work'],
  198:['a packaged macOS installation and controlled release registry','inspect version, update and roll back the inventoried channel','artifact selection and previous installation remain inspectable','failed download or incompatible runtime does not break the working legacy version'],
  199:['a clean native Linux installation and isolated home','install and launch the selected architecture artifact','real terminal and filesystem behavior matches the supported capability set','unsupported architecture and unavailable confinement fail clearly'],
  200:['a clean native Windows installation and isolated home','install and launch without WSL','real console, child process and filesystem behavior matches the supported capability set','unsupported terminal capability and installer failure remain explicit'],
  204:['a trusted plugin contributing an MCP server and tools','install, enable and disable the contribution','live MCP discovery and actual calls follow plugin lifecycle','untrusted plugin, restart failure and uninstall cannot leave active grants'],
  205:['a plugin contributing a Rhai workflow catalog','enable, launch, update and remove its workflow','catalog provenance and dsh child execution follow plugin lifecycle','invalid metadata and removed plugin leave no stale runnable entry'],
  201:['native macOS, Linux and Windows terminals with capability fixtures','exercise the inventoried platform-dependent path','reported capability agrees with actual terminal, audio, clipboard and confinement behavior','unsupported features refuse rather than emulate unsafe success'],
  206:['a deterministic research service with conflicting and missing sources','run the built-in research workflow','only verified claims include traceable citations; limitations remain visible','failed shard and missing evidence produce a partial report'],
  210:['synthetic prior sessions with unsupported and sensitive records','run the inventoried session-learning flow','selected content and memory provenance are explicit','unavailable provider and unsupported records never masquerade as complete learning'],
}
const contextual = [
  { id: 'PARITY-141-overlay', ticket: 141,
    given: 'an isolated installed ACP/CLI client with conflicting disk requirements/managed settings, inline/file overlays and process, network and trust canaries',
    when: 'inject valid soft settings alongside forbidden command, auth, endpoint, trust, discovery and child-env values via GROK_CONFIG and GROK_CONFIG_PATH',
    then: 'requirements and MDM remain authoritative; the fail-closed allowlist admits only documented soft settings and env filters; assert no unintended process spawn, network redirection or trust elevation, no injected child-env value, and unchanged raw-disk security gates',
    failure: 'empty or malformed inline input follows the documented file-overlay fallback with warning; invalid file syntax, forbidden nested fields and mixed valid/forbidden input never broaden authority or modify disk configuration' },
  { id: 'PARITY-152-vim-scrollback', ticket: 152,
    given: 'a real terminal with a long transcript, selected block, readable copy target and retained composer draft',
    when: 'toggle /vim-mode and ui.vim_mode, focus scrollback, exercise j/k, h/l, g/G, y/Y, resize and toggle off',
    then: 'scrollback navigation, selection and copy obey Vim mode; with it off a bare or shifted letter moves focus to the prompt and inserts that character; persistence does not change the separate ui.simple_mode editor setting',
    failure: 'mode switch, modal dismissal and unsupported terminal copy retain the reading location and draft; no navigation key submits a model turn' },
  { id: 'PARITY-193-claude-config', ticket: 193,
    given: 'synthetic foreign config with distinct permissions, environment, MCP, hook and path values, plus isolated target Home/Profile and hashes of the originals',
    when: 'open /import-claude, inspect and select permissions, environment variables, MCP servers, hooks and paths, then import or cancel with conflicting target settings',
    then: 'selected configuration copies and effective origins match the confirmed selection; original files are unchanged, no session histories are imported and executable contributions still pass normal trust/approval gates',
    failure: 'trust grants and credentials remain unchanged unless separately explicitly authorized; absent/malformed input, canceled modal and partial import are actionable without executing imported hooks or starting MCP processes' },
  { id: 'PARITY-164-environment', ticket: 164,
    given: 'a local recording hook and two isolated workspaces with synthetic session IDs and attempted reserved-variable overrides',
    when: 'trigger hooks after setting GROK_WORKSPACE_ROOT and CLAUDE_PROJECT_DIR in both inherited environment and hook env configuration',
    then: 'captured child values use the real workspace root and session/event identity; reserved values cannot be spoofed by hook configuration, and both workspace aliases agree',
    failure: 'rejected overrides produce the documented warning; failed spawn, cancellation and cross-workspace invocation cannot leak a stale identity or run outside the selected workspace' },
  { id: 'PARITY-137-children', ticket: 137,
    given: 'a delayed parent turn, two real controlled child tasks and an unrelated session with retained draft and queue',
    when: 'cancel the parent with ask, always_stop and always_continue settings; answer or dismiss the ask card, then reconnect and inspect child status/output',
    then: 'actual child process lifecycle and cancellation events match the selected policy; stopped children cannot keep making effects, continued children retain their owner and output, and unrelated session work survives',
    failure: 'stale approval, dismissed ask, already-completed child and disconnect never misreport a running child as stopped or apply cancellation to another session' },
  { id: 'PARITY-192-review-upload', ticket: 192,
    given: 'an external ACP client, synthetic code-review citations and a controlled outbound collector with consent disabled and explicitly enabled',
    when: 'send x.ai/review/comment with citation/session identity and x.ai/review/comment/delete for its tombstone; inspect protocol responses and actual outbound records',
    then: 'assert explicit consent and configured destination before any upload; create/tombstone payloads, IDs and session routing match recorded effects, with no plan approval or model revision implied',
    failure: 'protocol acknowledgement is not proof of upload completion; absent configuration, denied consent, expired auth, malformed requests and asynchronous upload failure cannot leak data or fabricate successful delivery' },
  { id: 'PARITY-164-prompt', ticket: 164,
    given: 'a recording local provider, durable synthetic session, queued prompts and hooks returning exit 2, JSON block, allow, crash and delay',
    when: 'submit a typed prompt, then edit/resend/discard it and restart the session; run automatic task/scheduler/child turns under the same hook',
    then: 'blocked text and reason stay out of provider context and durable history/summary; the queue remains suspended until explicit action; only live scrollback and local up-arrow history retain the blocked text; allowing stdout adds no context',
    failure: 'automatic and child turns are observe-only; the documented 30-second timeout and crash fail open rather than inventing a hard block; queue release never duplicates submission' },
  { id: 'PARITY-143-confinement', ticket: 143,
    given: 'isolated global hook/config/trust/requirements paths, allowed sibling canaries and workspace/read-only/strict/custom/devbox profiles on supported native platforms',
    when: 'attempt direct tool and shell writes, parent directory rename, hook-path retargeting and nested mount/user-namespace changes, then request session-only config changes',
    then: 'assert actual kernel write denial and unchanged protected bytes/parents while allowed siblings remain writable; protected config changes affect only the session, not saved global state; documented devbox exceptions remain distinct',
    failure: 'symlink components and missing registered hook targets refuse startup; wrong-type initial paths, unavailable kernel policy and unverified Linux read-only mounts fail closed before execution' },
  { id: 'PARITY-167-transport', ticket: 167,
    given: 'recording local MCP stdio, streamable HTTP and SSE servers with deterministic tools, status and failure responses',
    when: 'perform initialize/initialized handshake over stdio, HTTP and SSE, discover and call a qualified tool, toggle/restart it and change per-session configuration',
    then: 'assert actual request/response IDs, initialization order, tool results, session-ID header substitution, configured headers and transport destination; catalog/status agree with callable tools, not generic ACP or shell help',
    failure: 'disconnect/reconnect, stale session ID, malformed result, timeout, invalid tool names and canceled initialization produce explicit outcomes without duplicate tool effects' },
  { id: 'PARITY-168-remote', ticket: 168,
    given: 'a controlled remote MCP/OAuth service and synthetic bearer/header secrets, resources, typed content and elicitation',
    when: 'connect with configured transport/headers, renew authentication, read a resource, accept/decline elicitation and reconnect a session',
    then: 'record actual MCP requests and content results, header substitution and token destination; OAuth consent and session identity stay separate from unrelated model/service credentials',
    failure: 'expired token, invalid header, redirect to another origin, unsupported transport, malformed content, canceled/declined elicitation and reconnect cannot leak credentials or silently approve' },
  { id: 'PARITY-167-limits', ticket: 167,
    given: 'a delayed local MCP fixture with multibyte large results and distinct requirements, environment, project/user config, remote and default limit sentinels',
    when: 'vary startup/tool timeout and output-cap controls, including both compatibility/native aliases, then initialize and call real fixture tools',
    then: 'assert requirements precedence and server-specific overrides from the recorded contract; MCP_TIMEOUT milliseconds round upward before GROK_MCP_STARTUP_TIMEOUT_SECS fallback; GROK_MAX_MCP_OUTPUT_BYTES wins over MAX_MCP_OUTPUT_BYTES; effective config beats remote/default values; byte-safe truncation and full spill artifacts agree with actual results',
    failure: 'malformed, zero, negative and overflow values follow documented fallback without zero-time startup or unlimited output; timeout cancels initialization, spill failure is explicit and never presents missing output as complete' },
  { id: 'PARITY-140-headers', ticket: 140,
    given: 'a recording local inference provider, synthetic environment secrets and distinct global/provider/model static headers',
    when: 'select built-in, catalog and custom models with inherited or overridden headers, then send deterministic requests and inspect only fixture traffic',
    then: 'assert case-insensitive per-key model override and global-only inheritance, provider inheritance when model fields are absent, and environment-header precedence over static values; headers reach inference only, never media services or persisted secrets',
    failure: 'unset and blank environment values skip the header; invalid values fail explicitly, session rebuild refreshes values and canceled requests cannot leak to another destination' },
  { id: 'PARITY-147-updates', ticket: 147,
    given: 'two external ACP clients attached to one synthetic durable session with known text/tool/usage events and an independent session',
    when: 'subscribe to x.ai/session/update, request x.ai/session/updates replay with pagination, consume x.ai/session/updates/chunk and reconnect from a saved cursor',
    then: 'assert exact event order, chunk boundaries, completion marker and session/client routing; replay reconstructs the same history without duplicate execution or durable writes',
    failure: 'unknown/stale session or cursor, invalid page bounds, partial/out-of-order chunk, malformed event and disconnect return explicit errors or incomplete status; unrelated sessions receive no events' },
  { id: 'PARITY-179-review', ticket: 179,
    given: 'a real PTY plan review with line ranges, an active session and queued prompts, plus a recording revision provider',
    when: 'focus preview/comment/prompt, add inline and freeform feedback, request a revision, run a pager command and accept or dismiss the plan',
    then: 'revision receives only the intended comments and notes while plan mode remains active; line selection, review focus and unrelated queued input survive; implementation begins only after actual approval',
    failure: 'a pending complete pager command prevents accidental approval until run or removed; canceled feedback, provider failure and mode switch preserve the plan rather than submitting product telemetry' },
  { id: 'PARITY-154-announcements', ticket: 154,
    given: 'fullscreen/minimal PTYs with no announcements and with deterministic session banners; frozen-binary availability must first be established',
    when: 'inspect completion, invoke /announcements show/hide repeatedly, use invalid/empty arguments, switch sessions and resize',
    then: 'conditional visibility follows announcement presence, not hidden IDs; hide removes only the banner and show restores it, with the command still discoverable; draft/queue/session stay unchanged',
    failure: 'unavailable 1.0.34 command or unsupported mode remains explicit; invalid arguments show usage, missing banners do not fabricate content, and dismissal/quit restores terminal state' },
  { id: 'PARITY-159-location', ticket: 159,
    given: 'a dashboard in real PTYs, two temporary directories and an existing session; frozen-binary availability must first be established',
    when: 'open /cd with no argument, navigate/select/cancel the location picker, pass a path directly and dispatch a subsequent new session',
    then: 'the subsequent new-agent cwd equals the selected location while existing session cwd/draft/history remain unchanged; direct arguments and picker agree',
    failure: 'non-dashboard and unsupported-mode invocation gives the proper remedy; missing/unreadable path, dismissal, resize and canceled creation preserve the previous location and never launch work' },
  { id: 'PARITY-154-gboom', ticket: 154,
    given: 'an active synthetic session in fullscreen/minimal real terminals with supported/unsupported graphics; frozen-binary availability must first be established',
    when: 'invoke bare /gboom, exercise overlay controls, resize and dismiss; separately check argument passthrough against a recording local provider',
    then: 'only the bare command opens the session overlay; it remains hidden in completion; arguments pass through unchanged, dismissal restores session/draft/queue/focus without a spurious turn',
    failure: 'no-session, unsupported mode/graphics, invalid input and cancellation are explicit and leave an operable prompt; cleanup restores all terminal modes without orphan activity' },
]
export const acceptance = contextual.map(scenario => ({ ...scenario, status: 'planned', boundary: 'installed-product/public-protocol and controlled effects; source declarations are not measured frozen-binary parity' }))
for(const [number,values] of Object.entries(scenarios)){
 const [given,when,then,failure]=values
 acceptance.push({id:`PARITY-${number}`,ticket:Number(number),status:'planned',given,when,then,failure,boundary:'installed-product/public-protocol; exact discovery row is the parameter; reference observation required before candidate comparison'})
}
acceptance.push({id:'PARITY-145-input',ticket:145,status:'planned',given:'an installed headless product with synthetic UTF-8 prompt files, JSON content blocks and distinct system/rules sentinels',when:'invoke --prompt-file, --prompt-json, --single and system-prompt override/append aliases through a recording local provider',then:'decode the intended user content, preserve content-block types, and assert replacement versus append semantics in actual provider request roles and bytes',failure:'missing/unreadable file, malformed JSON, conflicting input sources and interruption return the reference exit/diagnostic without a provider call',boundary:'installed CLI and recording provider; no composer/caret surrogate'})
acceptance.push({id:'PARITY-154-fps',ticket:154,status:'planned',given:'equal real PTYs with GROK_FPS unset, 0 and 1 plus isolated animation.fps settings',when:'start and interact with the client, vary rendering cadence and resize, then quit',then:'FPS overlay visibility and actual rendering cadence follow the reference without becoming conversation content; normal terminal teardown succeeds',failure:'invalid rate, disabled animation and narrow terminal do not hang, corrupt frames or affect prompt text',boundary:'real terminal frames and monotonic timing, not CLI help'})
acceptance.push({id:'PARITY-154-tutorial',ticket:154,status:'planned',given:'fullscreen and minimal real PTYs with an existing session, draft, reading position and queued prompts',when:'open /tutorial, /tour and /onboarding; navigate topic list, open a topic, move next/previous, scroll/resize, and dismiss topic/list independently',then:'fullscreen shows the same interactive topic content and explored state; dismissal restores focus/session/draft/queue/reading state without creating a conversation turn; aliases are equivalent',failure:'minimal refuses with the fullscreen remedy rather than an invisible modal; Escape and invalid navigation preserve unrelated state and terminal teardown',boundary:'installed real terminal; visible topic frames, state preservation and mode refusal, not --help'})
acceptance.push({id:'PARITY-190-workspace',ticket:190,status:'planned',given:'a controlled configured hub, two isolated workspace directories and an explicitly selected leader PID',when:'exercise workspace start/pause/resume/stop/restart/status and list alias with cwd, leader/no-leader, PID, hub-url and JSON variants',then:'actual exposure/drain/reconnect state agrees with status JSON and selected workspace/owner; stop leaves the leader running as specified; restart preserves the requested configuration',failure:'disabled gate, invalid PID, rejected authentication, unreachable hub and interruption do not expose a different workspace or duplicate remote execution',boundary:'installed command interface and controlled remote service; help-only recognition is not execution evidence'})
acceptance.push({id:'PARITY-142-security-effects',ticket:142,status:'planned',given:'an isolated repository, protected canary files, synthetic MCP/web endpoints, remembered grants, and conflicting allow/ask/deny rules from CLI and every supported config scope',when:'exercise each documented mode and rule family against direct tools and real shell segments, including prefix/glob boundaries, chained commands, wrappers, lexical traversal, symlink targets, read-only exemptions and dangerous commands',then:'assert actual permitted effects and unchanged protected state on denial; observed prompts and grant persistence follow deny-over-ask-over-allow and the documented mode exceptions; nonmatching allow rules do not become a closed allowlist',failure:'malformed rules, unresolved paths, stale approvals, noninteractive ask and cross-project grants cannot silently authorize unintended effects; PreToolUse denial remains effective under automatic approval and hook allow does not override a deny',boundary:'installed product tool/approval lifecycle plus filesystem/process/service effects; not Skills discovery, OAuth login or composer editing'})
acceptance.push({id:'PARITY-154-help',ticket:154,status:'planned',given:'fullscreen and minimal real PTYs with a draft, queue and known command catalog',when:'open /help or Ctrl+P, filter the command palette, navigate results, select a local command and dismiss through Escape',then:'the visible filtered selection executes the intended command once; dismissal restores focus and unrelated draft/queue/session state; no model turn is submitted merely by opening help',failure:'empty/no-match query, mode-filtered commands, resize and cancellation leave an operable prompt and do not invoke a hidden selection',boundary:'real terminal command-palette interaction and session effects, not command-line --help'})
acceptance.push({id:'PARITY-154-docs',ticket:154,status:'planned',given:'fullscreen and minimal PTYs, bundled guide content and a controlled browser opener',when:'open /docs, /howto and /guides; filter/select/read/scroll a guide, use a title argument, dismiss nested views and exercise web target through the controlled opener',then:'the selected title and complete document render in the correct view; aliases agree and dismissal restores prompt/focus without sending a model request; explicit web target opens only the documented URL',failure:'unknown title, no-match search, narrow resize, opener failure and cancellation are actionable and preserve session/draft/queue',boundary:'real terminal picker/viewer and controlled OS opener; offline probe does not launch a personal browser'})
acceptance.push({id:'PARITY-154-debug',ticket:154,status:'planned',given:'real PTYs in each supported mode with synthetic transcript and an isolated diagnostic-output directory',when:'invoke /debug status, fps, scroll and log; toggle twice, use /scroll-debug alias, submit an invalid subcommand and quit',then:'reported toggle state agrees with actual overlays and local diagnostic file effects; changing diagnostics preserves prompt/transcript and release visibility rules; off removes the overlay',failure:'unsupported mode, invalid subcommand, unwritable log directory and resize return explicit outcomes and clean terminal teardown without sending data externally',boundary:'real terminal frames and actual local log effects; FPS-only observation does not verify every diagnostic variant'})
acceptance.push({id:'PARITY-152-dock',ticket:152,status:'planned',given:'real fullscreen/minimal PTYs with the dock gate disabled and enabled, multiple queued prompts, child agents, tasks and watchers',when:'establish frozen-binary availability, then focus/open/switch/close each consolidated dock pane, navigate selections, resize and switch sessions while items update',then:'only supported variants expose the dock; pane focus and selected item identify the correct live resource; dismissal restores draft/reading position and no queue/task is executed by navigation',failure:'disabled gate, empty or stale pane, canceled child/task and unsupported mode remain explicit with no invisible input trap or fabricated progress',boundary:'real terminal interaction plus queue/child/task state; availability is unverified until the feature-enabled frozen reference is exercised'})
